#include <stdio.h>
#include <stdlib.h>
#include <string>
#include <map>
#include <iterator>

using namespace std;

int main(){
	map<std::string, pair<int, pair<int, int> > > countryMedals;
	char countryName[11234];
	while(scanf("%s", countryName) != EOF){
		//Gold medal
		scanf("%s", countryName);

		//Silver medal
		scanf("%s", countryName);

		//Cooper medal
		scanf("%s", countryName);
	}

	return 0;
}
